# RDI Brand Guidelines — pointer

The canonical guidelines for the RDI brand live at a single location:

**`D:\Evercam rdi\brand\GUIDELINES.md`**

That file is the source of truth. Read it directly.

This pointer exists so that anyone browsing the skill folder finds the right path. It is intentionally **not** a copy — copies drift, single sources don't.

## If the canonical file isn't accessible

If the RDI portal folder (`D:\Evercam rdi\`) isn't mounted in the current session, the parent [`SKILL.md`](../SKILL.md) carries a summary of the rules sufficient for most tasks. Anything beyond that summary needs the canonical file or a check with the brand owner (Kamel Badji, `kamel.badji@evercam.io`).

## Governance

The brand owner is **Kamel Badji**, Ux/Ui/Brand designer. Brand-implementation decisions, exception requests, and updates to the canonical file are signed off by Kamel.

The CEO (Marco) is consulted on strategic direction but does not own brand-implementation decisions.
